<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="SwipeBackLayout">
        <item name="shadow_left">@drawable/drag_back_shadow_left</item>
        <item name="shadow_right">@drawable/drag_back_shadow_right</item>
        <item name="shadow_bottom">@drawable/drag_back_shadow_bottom</item>
    </style>
    <style name="CustomTitleTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowActionBarOverlay">false</item>
    </style>

    <style name="CustomTitleTheme.translucent" parent="CustomTitleTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@drawable/window_background</item>
    </style>
</resources>
